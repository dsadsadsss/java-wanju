#!/bin/bash

## ==========设置各参数（不需要的可以删掉或者前面加# ）==================
# 设置ARGO参数 (如果设置，注意把前面的# 去掉，如果不设置，默认启用临时隧道)
# export TOK='xxxxx'
# export ARGO_DOMAIN='xxxxx'

#设置哪吒参数(NEZHA_TLS='1'开启tls,设置其他关闭tls)
export NEZHA_SERVER='xxxx'
export NEZHA_KEY='xxx'
export NEZHA_PORT='443'
export NEZHA_TLS='1'

# 下面设置UUID和路径，CF_IP是优选IP，SUB_NAME为节点名称
export UUID='fd80f56e-93f3-4c85-b2a8-c77216c509a7'
export VPATH='vls'
export CF_IP='cdn.xn--b6gac.eu.org'
export SUB_NAME='bbn'

#设置端口（玩具平台不需要）
#export SERVER_PORT='3000'

# 设置启动玩具平台原程序，其他平台不需要,JAR_FLIE:改后的名字 ;JAR_SH：原启动命令前缀，JAR_SH2：原启动命令后缀
# export JAR_FLIE='senver.jar'
# export JAR_SH='java -Xms128M -XX:MaxRAMPercentage=95.0 -jar'
# export JAR_SH2=''
# 原程序启动，则订阅不可用
